{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run",
      "type": "shell",
      "command": "g++ -std=c++17 ${file} -o ${fileBasenameNoExtension} && ./${fileBasenameNoExtension} < input.txt > output.txt && git diff --quiet || (git add . && git commit -m \"run: ${file}\" && git push)",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}